services:
  mimo-tts-studio:
    image: ghcr.io/smathsp/mimo-tts-studio:main
    ports:
      - "3001:3001"
    environment:
      - MIMO_API_KEY=${MIMO_API_KEY}
    volumes:
      - ./data:/app/data
